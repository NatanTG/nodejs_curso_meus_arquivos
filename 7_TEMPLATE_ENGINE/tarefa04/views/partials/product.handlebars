{{!-- product.handlebars --}}
<h1>Product Detail</h1>

<div class="product">
    <h2>{{product.title}}</h2>
    <img src="{{product.image}}" alt="{{product.title}}">
    <p>{{product.content}}</p>
    <p>Preço: R${{product.price}}</p>
</div>